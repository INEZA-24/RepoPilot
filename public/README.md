# RepoPilot public assets

This directory contains public static assets used by the RepoPilot application.

## Planned brand asset

Add the final logo as:

`repopilot-logo.png`

The application branding work will reference this path so the logo can be added without changing the component code later.
